import { IsLastIndexOfPipe } from './is-last-index-of.pipe';

describe('IsLastIndexOfPipe', () => {
  it('create an instance', () => {
    const pipe = new IsLastIndexOfPipe();
    expect(pipe).toBeTruthy();
  });
});
